# Khai báo ý định: thư viện nào, khoảng phiên bản nào chấp nhận được.
# Ảnh Docker KHÔNG cài từ file này mà từ requirements.lock — xem ghi chú ở đó.
#
# Đổi gì ở đây thì phải sinh lại lock:
#   docker compose exec backend pip freeze > backend/requirements.lock
fastapi>=0.115,<1.0
uvicorn[standard]>=0.30,<1.0
sqlalchemy[asyncio]>=2.0.30,<2.1
asyncpg>=0.29,<1.0
alembic>=1.13,<2.0
pydantic>=2.7,<3.0
pydantic-settings>=2.3,<3.0
email-validator>=2.1,<3.0
PyJWT>=2.8,<3.0
passlib[bcrypt]>=1.7.4,<2.0
bcrypt>=4.0,<4.1
redis>=5.0,<6.0
slowapi>=0.1.9,<0.2
google-cloud-storage>=2.16,<3.0
pytest>=8.0,<9.0
pytest-asyncio>=0.23,<1.0
httpx>=0.27,<1.0
